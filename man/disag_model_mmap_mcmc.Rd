% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting_mcmc.R
\name{disag_model_mmap_mcmc}
\alias{disag_model_mmap_mcmc}
\title{Fit a multi-map disaggregation model via TMB + MCMC (tmbstan)}
\usage{
disag_model_mmap_mcmc(
  data,
  priors = NULL,
  family = "poisson",
  link = "log",
  time_varying_betas = FALSE,
  field = TRUE,
  iid = TRUE,
  silent = TRUE,
  starting_values = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{disag_data_mmap} object (from \code{prepare_data_mmap()}).}

\item{priors}{Optional named list of prior specifications (see internal helper).}

\item{family}{One of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, or \code{"negbinomial"}.}

\item{link}{One of \code{"identity"}, \code{"logit"}, or \code{"log"}.}

\item{time_varying_betas}{Logical; if TRUE, each time point has its own fixed-effect.}

\item{field}{Logical: include the spatial random field?}

\item{iid}{Logical: include polygon-specific IID effects?}

\item{silent}{Logical: if TRUE, suppress TMB/tmbstan output where supported.}

\item{starting_values}{Optional named list of starting parameter values.}

\item{verbose}{Logical: if TRUE, print total runtime.}

\item{...}{Additional arguments passed through to \code{tmbstan::tmbstan()}
(e.g., \code{chains}, \code{iter}, \code{warmup}, \code{cores}, \code{control}).}
}
\value{
An object of class \code{disag_model_mmap_mcmc} (a list with
  \code{$mcmc_fit}, \code{$obj}, \code{$data}, \code{$model_setup}, and
  \code{$engine_args_used}).
}
\description{
Builds the TMB ADFun object for a multi-map disaggregation model, then
fits the model via \code{tmbstan::tmbstan()}.
}
